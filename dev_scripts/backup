MARIADB_CONTAINER='mariadb'
BACKUP_FILE='/tmp/synclias.sql.gz'
docker exec ${MARIADB_CONTAINER} mariadb-dump --user ${MARIADB_USER} --password=${MARIADB_PASSWORD} ${MARIADB_DATABASE} | gzip --name ${BACKUP_FILE}
